import type { Meta } from '@storybook/react';
import { SchedulesPage } from './SchedulesPage';

const Story: Meta<typeof SchedulesPage> = {
  component: SchedulesPage,
  title: 'SchedulesPage',
};
export default Story;

export const Primary = {
  args: {},
};
