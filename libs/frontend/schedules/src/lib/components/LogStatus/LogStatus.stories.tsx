import type { Meta } from '@storybook/react';

import { LogStatus } from './LogStatus';

const Story: Meta<typeof LogStatus> = {
  component: LogStatus,
  title: 'LogStatus',
};
export default Story;

export const Primary = {
  args: {},
};
